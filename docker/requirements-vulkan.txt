# Frigate 0.17.2's requirements-wheels.txt, reduced to what a Vulkan/ncnn
# deployment actually loads. Dropped: tensorflow, tflite_runtime, openvino,
# degirum, the HailoRT block, transformers, sherpa-onnx/faster-whisper/librosa/
# soundfile, google-genai/ollama/openai, memray, mypy and opencv-contrib.
# onnxruntime stays: detectors/detection_runners.py imports it at module level.
# norfair and filterpy are installed separately with --no-deps; see Dockerfile.
aiofiles == 24.1.*
click == 8.1.*
aiohttp == 3.12.*
starlette == 0.47.*
starlette-context == 0.4.*
fastapi[standard-no-fastapi-cloud-cli] == 0.116.*
uvicorn == 0.35.*
slowapi == 0.1.*
joserfc == 1.2.*
cryptography == 44.0.*
pathvalidate == 3.3.*
markupsafe == 3.0.*
python-multipart == 0.0.20
onvif-zeep-async == 4.0.*
paho-mqtt == 2.1.*
pandas == 2.2.*
peewee == 3.17.*
peewee_migrate == 1.14.*
psutil == 7.1.*
pydantic == 2.10.*
pytz == 2025.*
pyzmq == 26.2.*
ruamel.yaml == 0.18.*
tzlocal == 5.2
requests == 2.32.*
setproctitle == 1.3.*
ws4py == 0.5.*
unidecode == 1.3.*
titlecase == 2.4.*
opencv-python-headless == 4.11.0.*
scipy == 1.16.*
onnxruntime == 1.22.*
py-vapid == 1.9.*
pywebpush == 2.0.*
pyclipper == 1.3.*
shapely == 2.0.*
rapidfuzz == 3.12.*
prometheus-client == 0.21.*
rich == 14.*
# frigate/util/services.py imports py3nvml at module level, so it is core even
# with no NVIDIA GPU present. Upstream installs it from git; PyPI is equivalent.
py3nvml == 0.2.*
# Only deepstack and the embeddings stack import PIL, both dropped here, but it
# is 3MB and keeps an optional-feature import from becoming a hard failure.
pillow
# frigate.app -> api.fastapi_app -> api.classification -> embeddings pulls the
# whole data_processing stack in at import time, so these are not optional even
# though a Vulkan deployment never uses semantic search or audio transcription.
regex
sherpa-onnx == 1.12.*
librosa == 0.11.*
# embeddings/onnx/jina_v1_embedding.py imports transformers at module level.
transformers == 4.45.*
# Stands in for the cp311-only tflite_runtime wheel; see docker/shims/.
ai-edge-litert == 2.1.*
# ncnn wheel deps, supplied here because it is installed --no-deps to keep its
# opencv-python requirement from pulling a second, libGL-linked cv2 over the
# headless one. The plugin only uses ncnn.Net, which needs neither.
tqdm
portalocker
